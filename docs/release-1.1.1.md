# Local Outline 1.1.1 更新说明

发布日期：2026-05-23

## 修复

- 修复演示模式进入全屏后背景变成黑色的问题。
- 全屏演示现在继续使用浅色文稿背景、正常文字颜色和浅色分隔线，避免黑色背景导致正文不可见。

## 验证

- `npm run build`
- 检查演示视图全屏样式仍使用 `var(--surface)` / `var(--surface-raised)` 背景。
- 提交前执行隐私扫描，确认未提交本机路径、真实 token 或私有配置。
