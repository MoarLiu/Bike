This placeholder keeps the planned Xcode project path reserved.

The implementation is currently a SwiftPM macOS app so it can compile in environments that only have Apple Command Line Tools. After full Xcode is available, this folder should be replaced by a real `LocalOutlineNative.xcodeproj` pointing at the existing source files under `Sources/LocalOutlineNative`.
