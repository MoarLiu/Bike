Golden fixtures for workspace/document JSON, Markdown, OPML, FreeMind, HTML, and PDF compatibility belong here.

The first implementation keeps tests programmatic. Before release, export real samples from the Electron 1.1.0 app and pin them in this folder.
